---
wifi:
  ssid: $wifi_ssid
  password: $wifi_password
  manual_ip:
    static_ip: $ip_address
    gateway: $gateway
    subnet: $subnet
  ap:
    ssid: "$name Hotspot"
    password: $ap_wifi_password
